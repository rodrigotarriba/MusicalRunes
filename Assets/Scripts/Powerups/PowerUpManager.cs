using System.Collections;
using System.Collections.Generic;
using UnityEngine;
 
/// <summary>
 /// Stores the enum and other options
 /// that will be used by the scriptableObject
 /// </summary>

namespace MusicalRunes
{
    //We generally like keeping this separate to have access to them in other functions.
    public enum PowerUpType 
    {
        hint, replay
    } 

}

